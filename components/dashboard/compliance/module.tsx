"use client"
import { Download, FileCheck2, ShieldCheck, TriangleAlert } from "lucide-react"
import { toast } from "sonner"
import { frameworks } from "@/lib/dashboard/data"
import { PageHeader, Panel, StatCard } from "@/components/dashboard/shared/ui"
import { Button } from "@/components/ui/button"
import { Badge } from "@/components/ui/badge"
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"
import { Progress } from "@/components/ui/progress"
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table"
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs"

const failed=[{ id:"AC-2.1", framework:"NIST CSF", control:"Privileged account review", owner:"IAM team", due:"Jul 24", status:"Overdue" },{ id:"CIS-4.8", framework:"CIS Controls", control:"Uninstall unauthorized software", owner:"Endpoint", due:"Jul 28", status:"In progress" },{ id:"PCI-10.2", framework:"PCI DSS", control:"Audit log completeness", owner:"Platform SOC", due:"Aug 02", status:"In progress" },{ id:"A.12.4", framework:"ISO 27001", control:"Event logging retention", owner:"SecOps", due:"Aug 05", status:"Scheduled" }]

export function ComplianceModule(){const overall=Math.round(frameworks.reduce((s,f)=>s+f.score,0)/frameworks.length);return <div className="flex flex-col gap-6"><PageHeader eyebrow="Governance, risk & compliance" title="Compliance posture" description="Continuous control monitoring, evidence collection, and audit readiness across regulatory frameworks." actions={<Button onClick={()=>toast.success("Executive compliance report generated")}><Download data-icon="inline-start"/>Executive report</Button>}/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard label="Compliance score" value={`${overall}%`} change="+3.2%" icon={ShieldCheck} detail="Across 6 frameworks"/><StatCard label="Controls passing" value="521 / 598" change="+11" icon={FileCheck2} detail="87% automated evidence"/><StatCard label="Failing controls" value="77" change="-9" icon={TriangleAlert} detail="4 overdue remediation"/><StatCard label="Evidence collected" value="1,284" change="+62" icon={FileCheck2} detail="Last sync 2 hours ago"/></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{frameworks.map(f=><Card key={f.name} className="bg-card/80"><CardHeader className="flex flex-row items-center justify-between pb-3"><div><CardTitle className="text-base">{f.name}</CardTitle><CardDescription>{f.controls} controls in scope</CardDescription></div><span className="font-mono text-xl font-semibold text-primary">{f.score}%</span></CardHeader><CardContent className="flex flex-col gap-3"><Progress value={f.score}/><div className="flex items-center justify-between text-xs text-muted-foreground"><span>{f.failed} failing</span><span>{f.evidence}% evidence</span><Button size="sm" variant="ghost" onClick={()=>toast.success(`${f.name} report downloaded`)}>Report</Button></div></CardContent></Card>)}</div><Panel title="Remediation & audit" description="Failing controls and audit activity requiring attention"><Tabs defaultValue="failed"><TabsList><TabsTrigger value="failed">Failing controls</TabsTrigger><TabsTrigger value="audit">Audit history</TabsTrigger></TabsList><TabsContent value="failed" className="pt-4"><div className="overflow-x-auto"><Table><TableHeader><TableRow><TableHead>Control</TableHead><TableHead>Framework</TableHead><TableHead>Owner</TableHead><TableHead>Due</TableHead><TableHead>Status</TableHead><TableHead className="text-right">Action</TableHead></TableRow></TableHeader><TableBody>{failed.map(c=><TableRow key={c.id}><TableCell><strong className="block text-sm">{c.control}</strong><span className="font-mono text-[11px] text-muted-foreground">{c.id}</span></TableCell><TableCell className="text-xs">{c.framework}</TableCell><TableCell className="text-xs">{c.owner}</TableCell><TableCell className="font-mono text-xs">{c.due}</TableCell><TableCell><Badge variant={c.status==="Overdue"?"destructive":"secondary"}>{c.status}</Badge></TableCell><TableCell className="text-right"><Button size="sm" variant="ghost" onClick={()=>toast.success(`Remediation task opened for ${c.id}`)}>Remediate</Button></TableCell></TableRow>)}</TableBody></Table></div></TabsContent><TabsContent value="audit" className="flex flex-col gap-3 pt-4">{[["SOC 2 Type II","External audit completed","Jul 12, 2026","Passed"],["ISO 27001","Surveillance audit","Jun 03, 2026","Passed"],["PCI DSS","Quarterly scan","May 28, 2026","2 findings"]].map(([f,e,d,r])=><div key={f} className="flex items-center justify-between rounded-md border bg-muted/30 p-3"><div><p className="text-sm font-medium">{f}</p><p className="text-xs text-muted-foreground">{e} · {d}</p></div><Badge variant={r==="Passed"?"secondary":"outline"} className={r==="Passed"?"text-primary":""}>{r}</Badge></div>)}</TabsContent></Tabs></Panel></div>}
