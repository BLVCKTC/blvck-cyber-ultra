"use client"
import { useMemo, useState } from "react"
import { Cloud, Laptop, Network, Search, Server, ShieldCheck } from "lucide-react"
import { assets } from "@/lib/dashboard/data"
import { PageHeader, Panel, Score, StatCard, StatusBadge } from "@/components/dashboard/shared/ui"
import { Button } from "@/components/ui/button"
import { Input } from "@/components/ui/input"
import { Badge } from "@/components/ui/badge"
import { Sheet, SheetContent, SheetDescription, SheetHeader, SheetTitle } from "@/components/ui/sheet"
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table"
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs"

export function AssetsModule(){const[q,setQ]=useState("");const[type,setType]=useState("All");const[selected,setSelected]=useState<(typeof assets)[number]|null>(null);const shown=useMemo(()=>assets.filter(a=>(type==="All"||a.type===type)&&`${a.name} ${a.owner} ${a.os}`.toLowerCase().includes(q.toLowerCase())),[q,type]);return <div className="flex flex-col gap-6"><PageHeader eyebrow="Attack surface management" title="Asset inventory" description="Continuously discover, classify, and prioritize endpoint, server, cloud, and SaaS exposure." actions={<Button><Network data-icon="inline-start"/>Run discovery</Button>}/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard label="Managed assets" value="12,842" change="+2.4%" icon={Laptop} detail="98.4% inventory coverage"/><StatCard label="Critical exposure" value="47" change="-8" icon={Server} detail="12 beyond remediation SLA"/><StatCard label="EDR coverage" value="97.8%" change="+0.6%" icon={ShieldCheck} detail="281 agents require attention"/><StatCard label="Cloud resources" value="3,219" change="+4.1%" icon={Cloud} detail="Across 18 accounts"/></div><Panel title="Enterprise inventory" description={`${shown.length} representative assets shown · data synchronized 38 seconds ago`} action={<Button variant="outline" size="sm">Export CSV</Button>}><div className="mb-4 flex flex-col gap-3 md:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground"/><Input className="pl-9" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search hostname, owner, IP, or OS…"/></div><div className="flex flex-wrap gap-2">{["All","Endpoint","Server","Cloud","SaaS"].map(v=><Button key={v} size="sm" variant={type===v?"default":"outline"} onClick={()=>setType(v)}>{v}</Button>)}</div></div><div className="overflow-x-auto"><Table><TableHeader><TableRow><TableHead>Asset</TableHead><TableHead>Type / OS</TableHead><TableHead>Criticality</TableHead><TableHead>Owner</TableHead><TableHead>Vulnerabilities</TableHead><TableHead>EDR</TableHead><TableHead>Health</TableHead><TableHead>Exposure</TableHead></TableRow></TableHeader><TableBody>{shown.map(a=><TableRow key={a.id} className="cursor-pointer" onClick={()=>setSelected(a)}><TableCell><strong className="block text-sm">{a.name}</strong><span className="font-mono text-[11px] text-muted-foreground">{a.id} · {a.ip}</span></TableCell><TableCell><span className="block text-xs">{a.type}</span><span className="text-[11px] text-muted-foreground">{a.os}</span></TableCell><TableCell><Badge variant={a.criticality==="Critical"?"destructive":"secondary"}>{a.criticality}</Badge></TableCell><TableCell className="text-xs">{a.owner}</TableCell><TableCell><span className={a.vulnerabilities>10?"font-mono text-destructive":"font-mono"}>{a.vulnerabilities}</span></TableCell><TableCell><StatusBadge value={a.edr}/></TableCell><TableCell><Score value={a.health}/></TableCell><TableCell><Score value={a.exposure} label="Risk"/></TableCell></TableRow>)}</TableBody></Table></div></Panel><Sheet open={!!selected} onOpenChange={o=>!o&&setSelected(null)}><SheetContent className="w-full overflow-y-auto sm:max-w-xl"><SheetHeader><SheetTitle>{selected?.name}</SheetTitle><SheetDescription>{selected?.id} · last seen 34 seconds ago</SheetDescription></SheetHeader>{selected&&<div className="flex flex-col gap-5 p-4"><div className="grid grid-cols-2 gap-3"><div className="rounded-md border p-4"><Score value={selected.health} label="Health score"/></div><div className="rounded-md border p-4"><Score value={selected.exposure} label="Exposure score"/></div></div><div className="flex flex-wrap gap-2">{selected.tags.map(t=><Badge key={t} variant="outline">{t}</Badge>)}</div><Tabs defaultValue="posture"><TabsList><TabsTrigger value="posture">Posture</TabsTrigger><TabsTrigger value="vulns">Vulnerabilities</TabsTrigger><TabsTrigger value="relations">Relationships</TabsTrigger></TabsList><TabsContent value="posture" className="pt-4"><dl className="grid grid-cols-2 gap-4 text-sm"><dt className="text-muted-foreground">Network location</dt><dd className="font-mono">{selected.ip}</dd><dt className="text-muted-foreground">Operating system</dt><dd>{selected.os}</dd><dt className="text-muted-foreground">Business owner</dt><dd>{selected.owner}</dd><dt className="text-muted-foreground">EDR agent</dt><dd>{selected.edr}</dd></dl></TabsContent><TabsContent value="vulns" className="pt-4"><p className="text-sm">{selected.vulnerabilities} open findings. Highest priority: CVE-2026-4421 with known exploit activity.</p></TabsContent><TabsContent value="relations" className="pt-4"><div className="rounded-md border p-4 text-sm">Connected to <strong>3 identities</strong>, <strong>7 services</strong>, and <strong>2 protected network zones</strong>.</div></TabsContent></Tabs><Button>Open remediation workflow</Button></div>}</SheetContent></Sheet></div>}
