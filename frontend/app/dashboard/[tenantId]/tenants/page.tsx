import { TenantsModule } from '@/components/dashboard/tenants/module'

export const metadata = { title: 'Tenants — BLVCK CYBER' }

export default function TenantsPage() {
  return <TenantsModule />
}
